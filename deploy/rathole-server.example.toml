# Rathole 服务端配置 - 公网机
# 接收内网机的连接

[server]
bind_addr = "0.0.0.0:2333"  # rathole 监听端口
default_token = "your-secret-token"  # 修改为强密钥，与客户端一致
heartbeat_interval = 30

# Next.js 应用 - Nginx 反代 127.0.0.1:1270
[server.services.acgn-app]
bind_addr = "127.0.0.1:1270"
