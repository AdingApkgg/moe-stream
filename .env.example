# ============================================================
# Docker/Podman Compose 配置（使用容器时设置）
# ============================================================
POSTGRES_USER="mikiacg"
POSTGRES_PASSWORD="mikiacg_password"
POSTGRES_DB="mikiacg"

# ============================================================
# 数据库连接
# ============================================================
# 本地开发: postgresql://user:password@localhost:5432/mikiacg?schema=public
# Docker:   由 compose.yaml 自动配置，无需设置
DATABASE_URL="postgresql://user:password@localhost:5432/mikiacg?schema=public"

# ============================================================
# Redis 连接
# ============================================================
# 本地开发: redis://localhost:6379
# Docker:   由 compose.yaml 自动配置，无需设置
REDIS_URL="redis://localhost:6379"

# ============================================================
# Better Auth
# ============================================================
# 生成密钥: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-key-here"
# 站点访问地址（Better Auth 需要此值处理回调/重定向）
# 开发环境: http://localhost:3000 | 生产环境: https://your-domain.com
BETTER_AUTH_BASE_URL="http://localhost:3000"

# Email (SMTP) - 可选
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM="noreply@example.com"

# App
# 开发环境: http://localhost:3000 | 生产环境: https://your-domain.com
# 注意: 站点 URL 和名称现在优先从后台「系统设置」中读取，此处值仅作 fallback
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Mikiacg"

# Uploads
UPLOAD_DIR="./uploads"

# IndexNow (搜索引擎快速索引) - 可选
# 生成密钥: openssl rand -hex 16
# 同时需要创建 public/{key}.txt 文件，内容为密钥本身
INDEXNOW_KEY=""

# Google Search Console API (可选，用于提交 Sitemap)
# 1. 创建 Google Cloud 项目并启用 Search Console API
# 2. 创建服务账号并下载 JSON 密钥
# 3. 在 Search Console 中将服务账号邮箱添加为用户
GOOGLE_SERVICE_ACCOUNT_EMAIL=""
GOOGLE_PRIVATE_KEY=""
