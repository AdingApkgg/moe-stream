apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: moestream

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - pvc.yaml
  - postgres.yaml
  - redis.yaml
  - app-deployment.yaml
  - socket-deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: moestream
